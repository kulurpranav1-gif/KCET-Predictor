apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: kcet-predictor-ingress

spec:
  ingressClassName: nginx

  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kcet-predictor-service
                port:
                  number: 80